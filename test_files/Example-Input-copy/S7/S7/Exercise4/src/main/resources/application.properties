# Example on setting information on server
# server.port = 8079

# Define the context path on which the API will be accessible
server.servlet.context-path=/exercise4

# Define the actuator health
management.endpoint.health.show-components=always
management.endpoint.health.show-details=always
